# @lang zh-CN BP 的可复现本地工具链：Node/pnpm 负责应用构建，Python 仅负责确定性字体维护；三者均由 mise 显式选择，避免依赖系统默认版本。
# @lang en Reproducible local BP toolchain: Node/pnpm build the app while Python is limited to deterministic font maintenance; mise selects all three explicitly instead of relying on system defaults.
[tools]
node = "24.12.0"
pnpm = "10.27.0"
python = "3.12.13"
